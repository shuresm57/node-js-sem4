# Svelte Client

This directory holds the client code, built on Svelte 5 with Javascript and plain CSS.

Libraries currently in use:

> svelte-routing (for private routes) \
> svelte-sonner (toast notification)

Run the client by runnng these commands:

```bash
$ npm install
$ npm run
```